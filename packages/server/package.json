{
  "name": "@rage-framework/server",
  "version": "0.0.1",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/katalist5296/rage-framework.git",
    "directory": "packages/server"
  },
  "bugs": {
    "url": "https://github.com/katalist5296/rage-framework/issues"
  },
  "scripts": {
    "build": "npm run clean && npm run compile",
    "build:watch": "tsc --watch --project tsconfig.build.json",
    "clean": "rm -rf ./dist",
    "compile": "tsc --project tsconfig.build.json",
    "test": "jest --config ../../jest.config.js",
    "test:watch": "jest --watchAll --config ../../jest.config.js"
  },
  "dependencies": {
    "@rage-framework/core": "^0.0.1",
    "typescript": "^4.1.3"
  },
  "devDependencies": {
    "ts-loader": "^8.0.13"
  },
  "publishConfig": {
    "access": "public"
  }
}
